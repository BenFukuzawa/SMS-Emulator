; CPU sanity check: a few hand-written programs.

(executable
 (name cpu_test)
 (modules cpu_test)
 (libraries sandbox))

; Z80 Instruction Set Exerciser (ZEXDOC / ZEXALL) harness.

(executable
 (name zex_runner)
 (modules zex_runner)
 (libraries sandbox))

; VDP port protocol: the $BE/$BF state machine. Exits nonzero on failure.

(executable
 (name vdp_test)
 (modules vdp_test)
 (libraries sandbox))

; SingleStepTests harness: per-opcode JSON truth data.

(executable
 (name sst_runner)
 (modules sst_runner)
 (libraries sandbox yojson))

; Renders one synthetic frame to a PPM. Eyeball check, not an assertion suite.

(executable
 (name vdp_frame)
 (modules vdp_frame)
 (libraries sandbox))

; Memory bus: Sega mapper + SMS memory map.

(executable
 (name mem_bus_test)
 (modules mem_bus_test)
 (libraries sandbox))

; I/O bus: SMS port decoding, with stub peripherals.

(executable
 (name io_bus_test)
 (modules io_bus_test)
 (libraries sandbox))

; The assembled console: a hand-written ROM driven through Machine alone.

(executable
 (name machine_test)
 (modules machine_test)
 (libraries sandbox))

; Boots a hand-assembled ROM on the whole machine and dumps the frame.

(executable
 (name machine_frame)
 (modules machine_frame)
 (libraries sandbox))

; Joypad: SMS controller port A/B decoding + key mapping.

(executable
 (name joypad_test)
 (modules joypad_test)
 (libraries sandbox))

; The debug view's derivations: disassembly, tile decode, sprite table.

(executable
 (name debug_test)
 (modules debug_test)
 (libraries sandbox))

; PSG: the SN76489 write protocol, tone frequencies, noise and mixing.

(executable
 (name psg_test)
 (modules psg_test)
 (libraries sandbox))

; Plays a short tune on the PSG and writes a WAV. Ear check, not assertions.

(executable
 (name psg_wav)
 (modules psg_wav)
 (libraries sandbox))

; Boots a real ROM and checks picture, sound and the debug view together.

(executable
 (name smoke)
 (modules smoke)
 (libraries sandbox))
