; Peripherals are added here as they are finished. Still out of the build:
; joypad, psg, machine, cart. io_bus is a functor over interface signatures
; (in shared/), so it builds without the concrete peripherals.
(dirs uints registers shared z80 vdp memory_bus cartridge ram io_bus)

(include_subdirs unqualified)

(library
 (name sandbox)
 (wrapped false)
 (libraries async core)
 (preprocess
  (pps ppx_jane)))
