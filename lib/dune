; Peripherals are added here as they are finished. Still out of the build:
; joypad, io_bus, psg, cartridge, machine, cart, memory_bus, ram.

(dirs uints registers shared z80 vdp)

(include_subdirs unqualified)

(library
 (name sandbox)
 (wrapped false)
 (libraries async core)
 (preprocess
  (pps ppx_jane)))
