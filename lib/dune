; Every peripheral subdirectory is in the build now.
;
; Note that (dirs) governs subdirectories only. machine.ml and cart.ml sit
; directly in lib/ and are always compiled, whatever this list says.

(dirs
 uints
 registers
 shared
 z80
 vdp
 memory_bus
 cartridge
 ram
 io_bus
 psg
 joypad
 debug)

(include_subdirs unqualified)

(library
 (name sandbox)
 (wrapped false)
 (preprocess
  (pps ppx_jane)))
